{
  "name": "bambi-beauty",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check app.js && node scripts/check.mjs"
  },
  "engines": { "node": ">=22" }
}
